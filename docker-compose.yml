services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: autoapi
      POSTGRES_USER: autoapi
      POSTGRES_PASSWORD: autoapi
    ports:
      - "5432:5432"
    volumes:
      - autoapi-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autoapi -d autoapi"]
      interval: 5s
      timeout: 3s
      retries: 10

  upstream-v1:
    build:
      context: ./tests/mock-upstream
    ports:
      - "9080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/v1/health"]
      interval: 10s
      timeout: 3s
      retries: 5

  gateway:
    build:
      context: ./Server
    ports:
      - "8080:8080"
    environment:
      AUTOAPI_R2DBC_URL: r2dbc:postgresql://postgres:5432/autoapi
      AUTOAPI_JDBC_URL: jdbc:postgresql://postgres:5432/autoapi
      POSTGRES_USER: autoapi
      POSTGRES_PASSWORD: autoapi
      AUTOAPI_CONTROLPLANE_ENABLED: "true"
    volumes:
      - ./deploy/config/runtime.json:/config/runtime.json:ro
    command:
      - "--autoapi.config.path=/config/runtime.json"
    depends_on:
      postgres:
        condition: service_healthy
      upstream-v1:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/readyz"]
      interval: 10s
      timeout: 3s
      retries: 15
      start_period: 40s

volumes:
  autoapi-postgres-data:

networks:
  default:
    name: autoapi-phase1
