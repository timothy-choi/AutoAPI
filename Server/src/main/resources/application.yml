server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  main:
    web-application-type: reactive

logging:
  level:
    root: INFO
    com.autoapi: INFO

autoapi:
  config:
    path: ${AUTOAPI_CONFIG_PATH:deploy/config/runtime.json}
